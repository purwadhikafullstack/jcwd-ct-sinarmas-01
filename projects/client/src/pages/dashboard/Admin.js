import { Button, Form, Drawer } from "react-daisyui";
import { useState } from "react";

export default function Admin () {
  const [openDrawer, setOpenDrawer] = useState(false);

  return (
    <>
      <h1 className="text-center text-2xl font-semibold mb-8">
        Admin Page
      </h1>
      <div className="p-3">
        
      </div>
    </>
  )
}